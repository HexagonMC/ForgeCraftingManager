stages:
  - deploy

deploy:
  stage: deploy
  script:
    - ./gradlew setupCiWorkspace
    - ./gradlew build
  only:
  - master
  - tags
  tags:
    - Java8
  artifacts:
    paths:
    - "build/libs/ForgeCraftingManager*.jar"
